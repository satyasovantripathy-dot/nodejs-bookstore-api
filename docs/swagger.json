{
  "openapi": "3.0.0",
  "info": {
    "title": "Bookstore API",
    "version": "1.0.0",
    "description": "Node.js Bookstore API",
    "contact": {
      "name": "st-lab-1-5 maintainer",
      "email": "satyasovan.tripathy@postman.com"
    }
  },
  "servers": [
    { "url": "/api" }
  ],
  "security": [
    { "ApiKeyAuth": [] }
  ],
  "paths": {
    "/books": {
      "get": {
        "summary": "List all books",
        "operationId": "getAllBooks",
        "tags": ["Books"],
        "x-contract-test-stub": "GET /books verifies 200 with non-empty books array",
        "responses": {
          "200": {
            "description": "A list of books",
            "content": {
              "application/json": {
                "schema": {
                  "type": "array",
                  "items": { "$ref": "#/components/schemas/Book" }
                }
              }
            }
          }
        }
      }
    }
  },
  "components": {
    "securitySchemes": {
      "ApiKeyAuth": {
        "type": "apiKey",
        "in": "header",
        "name": "x-api-key"
      }
    },
    "schemas": {
      "Book": {
        "type": "object",
        "properties": {
          "bookId": { "type": "string" },
          "title": { "type": "string" },
          "description": { "type": "string" },
          "publishedYear": { "type": "integer" }
        }
      }
    }
  }
}
